{
    "name": "aws-c-auth",
    "targets": {
        "android": {
            "enabled": false,
            "_comment": "disabled until we need to support it. LibCrypto needs to be configured on build machine."
        }
    },
    "upstream": [
        { "name": "aws-c-http" },
        { "name": "aws-c-cal" }
    ],
    "downstream": [
    ],
    "test_steps": [
        "test"
    ]
}
